# Next.js proxies browser API requests through the web origin.
BACKEND_URL=http://localhost:3001
# Socket.IO connects directly in local development. In a same-origin deployment,
# leave this empty and proxy /api/v1/socket.io with WebSocket upgrade support.
NEXT_PUBLIC_API_ORIGIN=http://localhost:3001
