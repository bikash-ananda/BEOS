{
  "name": "beos",
  "private": true,
  "description": "BEOS — Bikash Engineering Operating System",
  "scripts": {
    "dev": "pnpm --parallel --filter api --filter web run dev",
    "build": "pnpm --filter api build && pnpm --filter web build",
    "start": "pnpm --parallel --filter api --filter web run start"
  },
  "engines": {
    "node": ">=22"
  },
  "packageManager": "pnpm@10.34.5"
}
